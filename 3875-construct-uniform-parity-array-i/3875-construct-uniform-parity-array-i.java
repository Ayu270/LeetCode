class Solution {
    public boolean uniformArray(int[] nums1) {
        // int e =0;
        // for(int i=0; i<nums1.length; i++){
        //     if(nums1[i]%2 == 0)
        //       e++;
        // }
        // if(e == nums1.length || e == 0)
        //   return true;
        // int n2 [] = new int [nums1.length];
        // for()
        return true;
    }
}